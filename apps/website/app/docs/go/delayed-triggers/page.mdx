export const metadata = { title: "Go Delayed Triggers — Pingback Docs" };

# Delayed Triggers

Trigger a task with a delay using `pingback.WithDelay()`.

## Usage

```go
// Delay with a human-readable string
execID, err := pb.Trigger(ctx, "send-email", payload, pingback.WithDelay("15m"))

// Delay in seconds
execID, err := pb.Trigger(ctx, "send-email", payload, pingback.WithDelay(900))
```

## Delay Formats

| Format | Example | Description |
|--------|---------|-------------|
| Seconds (integer) | `900` | Delay by 900 seconds |
| Seconds | `"30s"` | Delay by 30 seconds |
| Minutes | `"15m"` | Delay by 15 minutes |
| Hours | `"2h"` | Delay by 2 hours |
| Days | `"1d"` | Delay by 1 day |
| Combined | `"1d2h30m"` | Delay by 1 day, 2 hours, 30 minutes |

Maximum delay: 30 days.
